# gitleaks per-finding allowlist. Each suppressed finding is one fingerprint
# on its own line, with a # comment line immediately above explaining why
# it's a false positive. Inline `# ...` comments on fingerprint lines are
# NOT supported by gitleaks — keep them above the fingerprint instead.
#
# Format: <commit-sha>:<file>:<rule-id>:<start-line>
#
# To add a new entry:
#   1. Run `nix run .#scan`. When a finding fires, gitleaks prints its
#      Fingerprint (or run `gitleaks git . -v` to see them explicitly).
#   2. Write `# Reason: <why>` then the fingerprint on the next line.

# Reason: k8s secretKeyRef.key field naming a field inside the
# stalwart-bootstrap-creds Secret — not a secret value, just the name
# of where the real DKIM private key lives.
46f267c2372598113d6fcdc3d55121c1a5b0b7e9:workloads/namespace/mail/apps/stalwart/init-job.nix:generic-api-key:286

# Reason: Helm chart `existingSecret:` field naming a k8s Secret by
# name — not a secret value, just a pointer to where the real secret
# lives. File has since been removed; finding only triggers on history.
54bfab95f047206064fc602c02c24d7a9a4f29ce:workloads/namespace/mumble/apps/mumble/helm.nix:generic-api-key:48
